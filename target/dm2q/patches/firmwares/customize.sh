LOG "- Adding GPU (Adreno 740) firmware"
LOG "- Adding Camera ICP firmware"
LOG "- Adding Display (EVASS) firmware"

# Note: Firmware files are automatically included from the vendor directory
# The files are copied as-is from the dump to maintain proper signatures
